version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
        - node server.js
  artifacts:
    baseDirectory: dist/juniordev
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
